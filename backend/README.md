# Backend (placeholder)

Use this folder for server-side code, for example:

- `src/`: application source files
- `config/`: environment and service configuration

Suggested next step:

1. Pick a stack (`Node.js/Express`, `Python/FastAPI`, etc.)
2. Add API endpoints under `src/`
3. Move external fetch/proxy logic from frontend into backend endpoints if needed
