{
  "name": "vivekanjan-com",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix frontend/react-app run dev",
    "build": "npm --prefix frontend/react-app run build",
    "preview": "npm --prefix frontend/react-app run preview"
  }
}
